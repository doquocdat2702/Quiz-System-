# Quiz System Frontend

React + Vite frontend for Quiz System.

## Commands

```bash
npm ci
npm run lint
npm test
npm run build
```

Set API URL with `VITE_API_URL`.

- Local backend dev: `VITE_API_URL=http://localhost:8080/api`
- Docker Compose with Nginx proxy: `VITE_API_URL=/api`
